# ─── Server ──────────────────────────────────────────────────
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# ─── PostgreSQL ───────────────────────────────────────────────
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB
DATABASE_URL=postgresql://syntrix:secret@localhost:5432/syntrix

# ─── Runner ──────────────────────────────────────────────────
# The synthetic runner service URL and shared secret
RUNNER_URL=http://localhost:4001
RUNNER_SECRET=syntrix-runner-secret-change-me

# ─── Slack Alerting ───────────────────────────────────────────
# Create a Slack app → Incoming Webhooks → copy URL here
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/REPLACE/WITH/YOUR_WEBHOOK

# ─── Email Alerting (Nodemailer) ──────────────────────────────
# Gmail: enable 2FA → App Passwords → generate one → use here
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=codingops2006@gmail.com
SMTP_PASS=kezi xpzl pjhm emhb

# Who receives alert emails (comma-separated for multiple)
ALERT_EMAIL_TO=aadharkulshrestha3150@gmail.com

# ─── Alert Behaviour ──────────────────────────────────────────
# Don't re-alert the same flow within this many seconds
ALERT_COOLDOWN_SECONDS=300
